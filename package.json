{
  "name": "Silentmoon",
  "version": "1.0.0",
  "description": "",
  "main": "./backend/server.js",
  "type": "module",
  "scripts": {
    "dev:server": "npm run dev2 --prefix backend",
    "dev:vite": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm:dev:*\"",
    "start": "npm run build --prefix frontend && npm start --prefix backend",
    "postinstall": "npm i --prefix backend && npm i --prefix frontend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
